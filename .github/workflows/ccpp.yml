name: C/C++ CI

on: [push]

jobs:
  build-ubuntu:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: build
      run: ./runBuild.sh
    - name: test
      run: cd build && ./bin/unit_tests

